# `after/` — pattern applied

Put the **refactored** solution here: same scenario as `before/`, structured with the pattern.

- Mirror the **same domain** as `before/` so readers can diff mentally or with their editor.
- Add only what the pattern needs; avoid “while we’re here” cleanups in unrelated code.
- If you use subpackages (e.g. `context/`, `strategy/`), keep them shallow and documented in the main `README.md`.

Optional: a tiny `App` or `main` that runs a demo helps, if the rest of the workshop uses that style.
